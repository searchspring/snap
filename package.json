{
  "name": "@searchspring/snap",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "lerna exec --parallel -- yarn run build",
    "test": "lerna exec --parallel -- yarn run test",
    "dev": "lerna exec --parallel -- yarn run dev"
  }
}
