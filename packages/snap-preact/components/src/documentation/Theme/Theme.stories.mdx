import { Meta, Markdown } from '@storybook/blocks';
import Readme from '../Theme/Theme.md';
import { highlightedCode } from '../../utilities';

<Meta title="Documentation/Theme" />

<style>
  {`
	.header {
		display: flex;
		align-items: center;
	}

	.header .subtitle {
		text-transform: uppercase;
		color: #00cee1;
	}

	.header img {
		width: 300px;
		margin-right: 20px;
	}
  `}
</style>

<div class="header">
	<span class="subtitle">SNAP PREACT COMPONENETS</span>
</div>

---

<Markdown  options={{
	overrides: {
		code: highlightedCode
	}
}}
>{ Readme }</Markdown>